# バックエンド用環境変数（任意）
# ALLOWED_ORIGINS=http://localhost:5200,http://127.0.0.1:5200

# Firebase Admin（IDトークン検証用）
# どちらかを設定してください
#
# 1) サービスアカウント JSON のパス（推奨）
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\serviceAccountKey.json
#
# 2) サービスアカウント JSON を文字列で直接渡す（改行を含む JSON のため扱い注意）
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
